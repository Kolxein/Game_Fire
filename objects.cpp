#include "objects.h"
#include "point.h"
#include "draw.h"

Objects::Objects(int a, int b, int c, int d)
	: itsX(a)
	, itsY(b)
	, itsHeight(c)
	, itsWidth(d)
{
};
//Object::position(int itsX, int itsY);
